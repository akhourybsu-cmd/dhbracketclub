
The user reports the new Home screen (with seasons, attention block, league snapshot, etc.) isn't appearing on mobile/PWA. Need to investigate root cause before proposing fixes.

Let me check what's actually in DashboardPage.tsx and whether the changes from the earlier "Command Center Refinement" plan were actually applied.
